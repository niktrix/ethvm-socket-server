import { expect } from 'chai'
import { describe, it } from 'mocha'

describe('zero.client.provider.factory', () => {
  describe('create() method', () => {
    it('should return true', () => {
      expect(true).to.be.true
    })
  })
})
